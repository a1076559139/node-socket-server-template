module.exports = {
    rml_client: {
        key: '',
        redis: {
            host: 'localhost',
            port: 6379,
            db: 0,
            // auth_pass?: string, 
            // password?: string
        }
    },
    rml_server: {
        key: '',
        redis: {
            host: 'localhost',
            port: 6379,
            db: 0,
            // auth_pass?: string, 
            // password?: string
        }
    }
};